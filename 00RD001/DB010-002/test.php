<?php 
	$sampleA = 5; $sampleB = 8;
	if ($sampleA >= 10 and $sampleA >= 10 ) 
	{ echo "変数sampleA,Bの内容は10以上。"; } 
	elseif ($sampleA >= 5  and $sampleB >= 5 )
	 { echo "変数sampleA,Bの内容は5以上10以下。"; } 
	elseif ($sampleA >= 3  and $sampleB >= 3 )
	 { echo "変数sampleA,Bの内容は3以上5以下。"; } 
	else 
	{ echo "変数sampleA,Bの内容は条件と一致せず。"; }
 ?>

